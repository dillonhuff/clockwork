kernel: dummy_app_opt
unroll factor: 1
burst width: 32

input uint32: u_off_chip(30, *)

local uint32: u(0, 0) = <NULLPTR FOR SODA COMPUTE STRING>

output uint32: dummy_app(0, 0) = (u(0, 0) - u(0, -1))


iterate: 1

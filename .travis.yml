dist: xenial
language: c++
sudo: true

addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - g++-4.9

before_install:
  - ./install_deps_rice.sh

script:
  - ./travis_build.sh

compiler:
  - gcc

os:
- linux
